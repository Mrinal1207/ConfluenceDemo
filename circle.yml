# For the deployment section 

deployment:
  dockerhub:
    branch: master
    commands:
      - $DOCKER_CIRCLECI_TRG

test:
  override:    
    - mocha
    
   notify:
       webhooks:
        - url: https://api.opsgenie.com/v1/json/circleci?apiKey=c4a2e75d-e470-4b70-a373-d1ebdb8e6369